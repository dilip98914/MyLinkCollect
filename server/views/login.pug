extends layouts/login-layout.pug
block content 
    .loginContainer
        h1 Login
        form#loginForm(method='post',onsubmit="event.preventDefault();validateForm();")
            p.errorMessage #{errorMessage}
            //- input(type="text", name="logUsername",placeholder="username or email", value=logUsername required="")
            //- input(type="password", name="logPassword",placeholder="password",value=logPassword required="")
            input(type="text", name="logUsername",placeholder="username or email", value='dilip98914@gmail.com' required="")
            input(type="password", name="logPassword",placeholder="password",value='dilip123' required="")
            input(type="submit", value="login")
        a(href="/register") Need an account? Register here...

    script.
        const passFields=document.getElementById('password')
        const form=document.getElementById('loginForm')   

        function validateForm(){
            form.submit()
        }